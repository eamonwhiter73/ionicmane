export declare type OutputFormat = 'jpeg' | 'png';
export interface CompressOptions {
    quality?: number;
    maxWidth?: number;
    maxHeight?: number;
    outputFormat?: OutputFormat;
}
export declare function compress(source: Blob, options?: CompressOptions): Promise<Blob>;
